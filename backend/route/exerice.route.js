const express = require('express');
const router = express.Router();
const exerciceController = require('../controller/exercice.controller');

// router.get('/exercices', exerciceController.getAllExercices);
// router.get('/exercice/:id', exerciceController.getExerciceById);
// router.post('/exercice', exerciceController.createExercice);
// router.put('/exercice/:id', exerciceController.updateExercice);
// router.delete('/exercice/:id', exerciceController.deleteExercice);

module.exports = router;